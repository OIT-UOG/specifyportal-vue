version: '3'
services:
  web:
    build: "https://github.com/OIT-UOG/specify-webportal-installer.git#vue-rewrite"
    volumes:
      - ./dist/:/usr/local/tomcat/webapps/apps/viewer/:ro
    ports:
      - "80:8080"